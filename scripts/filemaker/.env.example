# FileMaker Server Configuration
# Copy this file to .env and fill in your actual values

# FileMaker Server URL (no trailing slash)
FM_BASE_URL=https://your-server.example.com

# FileMaker Database Name (without .fmp12 extension)
FM_DATABASE=YourDatabaseName

# FileMaker Credentials (user must have 'fmrest' extended privilege)
FM_USERNAME=your_username
FM_PASSWORD=your_password

# Notes:
# - The user account MUST have the 'fmrest' extended privilege enabled
# - Use HTTPS (required for Data API)
# - Database name is case-sensitive
