# FileMaker Migration Data

This directory contains data from various stages of the FileMaker migration:

- **discovery/** - Schema discovery output (JSON files with layout/field info)
- **export/** - Raw exported data from FileMaker
- **transformed/** - Transformed data ready for PostgreSQL import
- **validation/** - Validation reports and logs

All data files are git-ignored for security.

